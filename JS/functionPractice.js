// Check if a Number is Even or Odd

function check(num){
    if(num%2==0){
        console.log(`${num} is even `);
    }
    else 
    console.log(`${num} is odd`);
}

check (0)
const User ={
    username : "yash"
    
}